# Backup directory path
BACKUP_DIR="/var/backups/db"

# MySQL username
MYSQL_USER="root"

# MySQL password
MYSQL_PASSWORD=""

# Databases to be excluded from backup
CONFIG_DB_EXCLUDE="mysql|information_schema|performance_schema|sys|test"

# Email option: attachments, no_attachments, no_email
# no_attachments: Default - send mail without attachments
# no_email: No - do not send email
# attachments: Yes - send mail with attachments
EMAIL_OPTION="no_attachments"

# Sender name for email notifications
SENDER_NAME="Loibv"

# Email address to receive notifications
EMAIL_TO="buivanloi.2010@gmail.com"